using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter your name: ");
        string name = Console.ReadLine();

        name = name.Trim();

        Console.WriteLine("Clean Name: " + name);
    }
}
