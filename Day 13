using System;

class Program
{
    static void Main()
    {
        string str = "Du@shy#ant#123!";
        string result = "";

        foreach (char ch in str)
        {
            if (char.IsLetterOrDigit(ch))
            {
                result += ch;
            }
        }

        Console.WriteLine("Result: " + result);
    }
}